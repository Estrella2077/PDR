# 基于移动终端智能设备的实时 IMU-PDR 软件开发

副标题：位置服务与应用课程项目汇报文稿

适用用途：
- 直接作为课程汇报 PPT 的文字底稿
- 按页拆分后可形成 12 到 16 页左右的答辩型 PPT
- 整体结构参考你提供的 `第4小组(1).pptx`：软件介绍、传感器采集、实时 PDR、结果总结

---

## 1. 封面页

标题：

基于移动终端智能设备的实时 IMU-PDR 软件开发

可放内容：
- 课程名称
- 小组名称与成员
- 指导教师
- 日期

可配图建议：
- 手机界面截图
- 地图轨迹截图
- IMU / GNSS / PDR 三要素图标

---

## 2. 项目背景与研究意义

室内外过渡环境、遮挡环境和弱 GNSS 环境下，单纯依赖卫星定位会出现定位不稳定、更新不连续、轨迹跳变等问题。行人航位推算 PDR 可以利用手机自身的加速度计、陀螺仪和磁力计，在短时间内连续估计用户的航向、步数、步长和位移，实现不依赖持续卫星信号的相对定位。

本项目面向 Android 手机，完成了一个集“传感器采集、实时 PDR、地图显示、GNSS 融合、数据保存、离线后处理”于一体的课程型原型系统。与只做离线算法验证的作业相比，本项目强调“手机端实时运行”和“完整软件链路落地”。

可以强调的价值：
- 验证 IMU-PDR 从理论到移动端实现的完整流程
- 搭建可重复采集、可重复回放、可调参数的实验平台
- 为后续滤波融合、模型优化和多传感器扩展提供基础

---

## 3. 项目目标与总体功能

本项目的核心目标是：在手机端实时采集九轴 IMU 与 GNSS 数据，并基于手机端算法实时完成行人航迹推算，同时在地图上显示轨迹结果，并支持数据导出与离线复现。

系统当前已经实现以下功能：
- 实时采集加速度计、陀螺仪、磁力计和 GNSS 数据
- 基于九轴 AHRS 进行姿态解算和航向估计
- 基于加速度模长峰值法进行步态检测
- 基于步频与身高补偿模型进行步长估计
- 基于航向角与步长进行二维位置递推
- 支持 `PDR`、`GNSS`、`PDR+GNSS` 三种模式显示
- 支持地图轨迹实时绘制与箭头方向显示
- 支持保存 `imu_raw.csv` 与 `pdr_steps.csv`
- 支持重新导入数据并执行离线后处理

---

## 4. 系统总体架构

项目整体采用“数据采集层 - 算法处理层 - 可视化展示层 - 数据存储层”的结构。

### 4.1 数据采集层

负责调用 Android 传感器与定位接口，获取：
- 加速度计
- 陀螺仪
- 磁力计
- GNSS 位置与卫星状态

### 4.2 算法处理层

核心由以下模块组成：
- `AhrsEstimator.kt`：九轴 AHRS 姿态解算
- `PdrProcessor.kt`：步态检测、步长估计、位置递推
- `GeomagneticHelper.kt`：磁偏角修正
- `ImportedTrackParser.kt`：离线导入与后处理

### 4.3 展示层

`MainActivity.kt` 统一完成：
- 地图页面显示
- 传感器曲线显示
- 参考页面显示
- 设置页面参数调节

### 4.4 数据存储层

`CsvSessionWriter.kt` 负责：
- 原始 IMU/GNSS 数据保存
- 步级结果保存
- 轨迹回放所需元数据写入

---

## 5. 软件界面设计

软件界面采用四页面结构，兼顾演示性和实验性。

### 5.1 地图页

地图页提供以下功能：
- 启动、停止、重置实时定位
- 长按地图设置 PDR 起点
- 实时显示当前位置、航向、步数、距离和卫星状态
- 绘制 PDR、GNSS 或融合轨迹
- 显示当前朝向箭头

### 5.2 传感器页

传感器页展示以下实时信息：
- 加速度计三轴曲线
- 陀螺仪三轴曲线
- 磁力计三轴曲线
- PDR 趋势曲线
- 保存数据与导入后处理按钮

### 5.3 参考页

参考页主要用于辅助观测定位环境与姿态状态：
- 卫星天空图
- 参与定位卫星数量
- 星座分布情况
- 当前持机姿态摘要
- Pitch / Roll / 航向信息

### 5.4 设置页

设置页用于模型调参与显示控制：
- 身高参数
- 步长缩放 `stepLengthScale`
- 是否显示实时轨迹
- 是否显示导入后处理轨迹
- 导航模式切换

---

## 6. 传感器数据采集与保存

项目通过 Android `SensorManager` 与 `LocationManager` 获取手机端实时观测数据。采集链路覆盖了 IMU 与 GNSS 两类信息，其中：
- IMU 用于姿态解算、步态检测和短时位移估计
- GNSS 用于起点初始化、卫星观测显示和融合定位

数据保存分为两类文件：

### 6.1 `imu_raw.csv`

记录原始观测与实时状态信息，包括：
- 传感器类型
- 时间戳
- 三轴数值
- 精度信息
- 当前步数
- 当前局部坐标

该文件的价值在于：
- 可用于课后算法复现
- 可用于参数调试
- 可用于离线后处理验证

### 6.2 `pdr_steps.csv`

记录每一步产生后的结果，包括：
- 步序号
- 航向角
- 步长
- 局部坐标
- 累计距离

该文件更适合直接做轨迹回放和结果分析。

### 6.3 元数据设计

当前保存文件还会写入：
- 起点经纬度
- 身高参数
- 步长缩放系数
- 当前导航模式

这样做的意义是：后续重新导入时，可以尽量保持和实时运行阶段一致的算法参数。

---

## 7. 实时 PDR 算法流程

本项目实时 PDR 链路可以概括为：

1. 获取手机 IMU 数据
2. 估计当前姿态与航向
3. 从加速度中提取线性运动强度
4. 检测步态峰值
5. 估计步长
6. 按航向和步长递推位置
7. 投影到地图上进行轨迹显示

整个过程在手机端实时完成，不依赖服务器计算。

---

## 8. 航向初始化与九轴 AHRS 姿态解算

航向估计是 PDR 中最关键的环节之一。若航向不稳定，轨迹会快速发散。

### 8.1 初始化思路

项目采用基于加速度计与磁力计的静稳初始化方法：
- 加速度计用于估计重力方向
- 磁力计用于估计地磁方向
- 在检测到设备足够静止后，对一段窗口内的数据求平均
- 通过旋转矩阵求得初始姿态

这样做的好处是：
- 比拿到第一帧就初始化更稳
- 可以减少初始抖动带来的方向偏差
- 更符合课程参考材料中的静止初始化思路

### 8.2 九轴 AHRS 解算

初始化完成后，系统使用陀螺积分进行连续姿态更新，并利用重力与地磁观测进行反馈校正。项目中姿态以四元数形式维护，并输出：
- Yaw
- Pitch
- Roll

相较于只使用加速度计和磁力计的做法，九轴 AHRS 的优点是：
- 短时动态响应更快
- 转向过程更平滑
- 可在动态过程中抑制部分姿态噪声

### 8.3 磁偏角修正

由于磁北与真北并不一致，项目引入当地磁偏角进行修正，将磁航向转换为真北航向，从而提高地图方向的一致性。

---

## 9. 步态检测方法

项目中的步态检测基于“线性加速度模长 + 三点峰值检测 + 节律约束”的方法。

### 9.1 线性加速度提取

对原始加速度信号做重力分离：
- 先用低通方式估计重力分量
- 再从原始加速度中减去重力
- 得到反映身体运动强度的线性加速度

然后计算三轴线性加速度模长，作为步态检测的主要观测量。

### 9.2 峰值检测

系统维护最近三个运动强度样本：
- 若中间样本大于前后样本
- 且峰值高于阈值
- 且峰谷差足够大

则认为该样本可能对应一步。

### 9.3 节律约束

为避免短时抖动被误判为步态，项目额外加入：
- 最小步间隔约束
- 最大步间隔约束
- 与前一步间隔的节律一致性检查

这使得步态检测在一般行走过程中更稳定。

---

## 10. 步长估计模型

步长估计直接影响轨迹长度，是本项目近期优化的重点。

### 10.1 当前步长模型

当前项目已经采用与 `PDR-main` 一致的步长公式主干，并保留了本项目自己的参数调节能力。其核心思想是：步长同时受身高和步频影响。

可写为：

```text
L = [0.7 + 0.371 * (h - 1.75) + 0.227 * (f - 1.79) * h / 1.75] * s
```

其中：
- `L` 为步长，单位米
- `h` 为身高，单位米
- `f` 为步频，单位 Hz
- `s` 为步长缩放系数 `stepLengthScale`

### 10.2 步频估计方式

当前实现中，步频由最近步间隔得到：
- 当前步频 `f_now = 1000 / intervalMs`
- 上一步步频 `f_prev = 1000 / previousIntervalMs`
- 最终使用加权形式平滑

即：

```text
f = 0.65 * f_now + 0.35 * f_prev
```

这种方式能够降低单次误差对步长的直接影响。

### 10.3 步长缩放参数

考虑到不同手机、不同持机方式和不同用户步态会带来系统性偏差，项目额外提供了可调系数 `stepLengthScale`。

当前软件中：
- 该参数已经开放到设置页
- 默认值为 `0.67`
- 可根据实测距离进行标定

其意义在于：
- 保留 `PDR-main` 公式的结构优势
- 又能针对本项目设备差异做快速校准

---

## 11. 位置递推与地图显示

在每次检测到有效步态后，系统根据当前航向角和步长进行平面坐标递推：

```text
x(k) = x(k-1) + L * cos(theta)
y(k) = y(k-1) + L * sin(theta)
```

其中：
- `L` 为本步步长
- `theta` 为当前航向角

得到局部平面坐标后，再结合起点位置投影回地图坐标，用于轨迹显示。

项目中同时完成了以下坐标处理：
- 起点经纬度设定
- 局部平面坐标递推
- 平面坐标到地理坐标的反投影
- 地理坐标到百度地图坐标的显示转换

---

## 12. GNSS 融合策略

除了纯 PDR 模式外，项目还提供 `PDR+GNSS` 模式。

当前融合方法不是复杂的卡尔曼滤波，而是面向课程项目的轻量级精度加权融合：
- GNSS 精度好时，提高 GNSS 权重
- GNSS 精度差时，更依赖 PDR 连续性
- GNSS 长时间未更新时，回退到 PDR 结果

这样做的优点是：
- 实现简单
- 可解释性强
- 在课程项目中便于展示“绝对定位 + 相对定位”的结合思路

---

## 13. 离线后处理与结果复现

本项目不仅能实时运行，还支持离线后处理。

### 13.1 导入原始数据重跑算法

导入 `imu_raw.csv` 后，系统会：
- 重新解析原始观测
- 按与实时链路一致的 PDR 算法重新计算轨迹
- 在地图上显示后处理结果

这意味着项目具备：
- 算法复现实验能力
- 参数回放能力
- 实时结果与离线结果对比能力

### 13.2 导入步级文件直接回放

导入 `pdr_steps.csv` 后，系统可以直接根据步级坐标恢复轨迹，适合展示最终结果。

### 13.3 参数一致性设计

当前导入链路会优先读取保存时的：
- 身高
- 步长缩放系数
- 起点信息

因此导入结果与实时运行结果具有较好的一致性。

---

## 14. 关键实现亮点

相较于简单的课程作业版本，本项目的实现亮点主要有以下几点：

### 14.1 从算法验证走向完整软件链路

不是只做 Matlab/Python 离线处理，而是完整实现了：
- 手机端实时采集
- 手机端实时计算
- 手机端地图显示
- 手机端数据导出与导入

### 14.2 采用九轴 AHRS 而不是简单方向角估计

这使系统在动态过程中更具连续性，也更符合实际移动端使用场景。

### 14.3 引入磁偏角修正

使航向结果更贴近真实地图方向，而不是停留在磁北参考下。

### 14.4 支持多模式结果展示

不仅有纯 PDR，还包括：
- 纯 GNSS
- PDR+GNSS 融合

更适合课程展示定位方式的对比。

### 14.5 支持离线后处理和参数回放

使项目不仅是演示软件，也是一个实验平台。

---

## 15. 当前效果与实验现象

从当前项目状态看，系统已经能够完成：
- 手机端实时轨迹显示
- 实时方向显示
- 轨迹保存与回放
- 离线后处理复现

在实验中观察到的典型现象包括：
- 纯 GNSS 轨迹在弱环境下会有抖动
- 纯 PDR 轨迹连续但会随时间累积误差
- 融合模式在连续性和稳定性之间取得了一定折中
- 步长模型对总轨迹长度影响非常明显

当前为了让本项目与参考项目的同路段结果更接近，已经将默认步长缩放系数调整为 `0.67`，并开放到设置页供后续实测标定。

---

## 16. 存在问题与不足

虽然系统已经完成了课程项目所需的主要链路，但仍存在以下局限：

### 16.1 航向仍受磁场环境影响

在室内强磁干扰或手机姿态变化较大的情况下，航向仍可能漂移。

### 16.2 PDR 长时间运行会累积误差

由于没有采用更强的全局约束或滤波框架，长时间运行后轨迹仍会偏移。

### 16.3 步长参数存在设备差异

不同手机、不同握持方式、不同用户步态下，步长模型都可能需要重新缩放标定。

### 16.4 融合方法仍较轻量

当前只是按精度做权重融合，尚未实现更系统的状态空间滤波。

---

## 17. 后续优化方向

后续可以从以下几个方向继续改进：

### 17.1 航向鲁棒性优化

- 增强磁异常检测
- 优化静止初始化与重初始化策略
- 引入更强的姿态约束机制

### 17.2 步态检测优化

- 增加起步状态判断
- 提升慢走与晃动场景区分能力
- 引入更稳健的多步确认机制

### 17.3 步长模型优化

- 采集更多真实样本进行标定
- 分用户建立个性化缩放参数
- 尝试融合速度、摆动幅度等更多特征

### 17.4 融合算法优化

- 尝试卡尔曼滤波或因子图方法
- 引入地图约束
- 引入 Wi-Fi / 蓝牙等外部定位辅助

---

## 18. 汇报总结页

本项目完成了一个基于 Android 手机的实时 IMU-PDR 原型系统，实现了从数据采集、姿态解算、步态检测、步长估计、位置递推到地图显示、数据保存和离线后处理的完整链路。

项目的主要成果可以总结为：
- 实现了九轴 IMU 与 GNSS 的实时采集
- 实现了手机端实时 PDR 轨迹推算
- 实现了地图可视化与多模式显示
- 实现了离线后处理与实验回放
- 实现了可调步长缩放参数，便于实测标定

一句话总结可用于结尾：

本项目不仅验证了 PDR 基本原理，也完成了一个能够在手机上直接运行、直接演示、直接复现实验结果的课程级定位软件原型。

---

## 19. 建议的 PPT 配图清单

下面这些图建议你在做 PPT 时补上：
- 软件首页或地图页总览截图
- 传感器页三轴曲线截图
- 参考页卫星天空图截图
- 设置页中“身高 + 步长缩放”截图
- 实时 PDR 轨迹截图
- GNSS 与 PDR+GNSS 对比截图
- `imu_raw.csv` 与 `pdr_steps.csv` 文件结构示意图
- PDR 算法流程图
- 步长公式页的公式框

---

## 20. 可直接拆页的建议页序

如果你想快速做成 PPT，建议按下面顺序拆页：

1. 封面
2. 背景与意义
3. 项目目标与功能
4. 系统总体架构
5. 软件界面设计
6. 传感器采集与数据保存
7. 实时 PDR 总体流程
8. 航向初始化与九轴 AHRS
9. 步态检测
10. 步长估计模型
11. 位置递推与地图显示
12. GNSS 融合与离线后处理
13. 当前效果展示
14. 存在问题与后续优化
15. 总结

